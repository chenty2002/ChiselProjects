# CTL property used for model checking of LTL properties with terminal
# automata. This CTL property is used when backward analysis is desired.
# Otherwise, one should use invariant.ctl.

!EF(fair=1);
