#FAIL:
G(Busy=1 -> F Busy=0);
#PASS:
G(WCtrlDataStart=1 -> X Busy=1);
#PASS:
G(WCtrlDataStart=1 -> X MdoEn=1);
#PASS:
G(RStatStart=1 -> X Busy=1);
#PASS:
G(RStatStart=1 -> X MdoEn=1);
#PASS:
G(MdoEn=1 -> Busy=1);
#FAIL:
G(Busy=1 -> MdoEn=1);
#PASS:
G(EndBusy=1 -> X Busy=0);
#PASS:
G(EndBusy=1 -> X EndBusy=0);
#FAIL:
G(SyncStatMdcEn=1 + F Busy=0);
