Files in this directory:

----- matrix -----
This is a model of a matrix that hols a symmetric, reflexive relation.
The implementation stores only the lower triangle (diagonal excluded).
If running vis-1.3 or older, use flip.pl to reverse the order of the
.names lists.

matrix.v
matrix.mv
matrix.ord
matrix.ctl
matrix.vec


----- matrix_bug -----
This is a buggy version of matrix illustrating an interesting trap of
Verilog.  For the order and properties, use matrix.{ord,ctl}.

matrix_bug.v
matrix_bug.mv
