# A cell doesn't lock the token forever
!(P0.procState=lock);

!(P1.procState=lock);

!(P2.procState=lock);

!(P3.procState=lock);

!(P4.procState=lock);

!(P5.procState=lock);

!(P6.procState=lock);

!(P7.procState=lock);
