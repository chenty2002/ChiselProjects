# Starvation
G((status_A=car_waiting) -> F (status_A=cars_passing));

# Starvation 2
G((starv.state = NOT_OK) -> F (starv.state =OK));

# Safety
G(!(status_A=cars_passing) + ! (status_B=cars_passing));
