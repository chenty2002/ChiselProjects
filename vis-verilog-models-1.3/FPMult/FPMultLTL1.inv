# This invariant is used to check LTL properties with terminal automata.
# It is used when forward analysis (check_invariant) is desired.
# Otherwise, EFfair.ctl should be used with model_check instead.

fair = 0;
